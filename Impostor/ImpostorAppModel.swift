//
//  ImpostorAppModel.swift
//  Impostor
//
//  Created by William Entriken on 2024-02-08.
//

import Foundation
import SwiftUI

/*
struct ImpostorAppModel {
    
    struct Player: Equatable, Hashable {
        typealias Role = ImpostorGame.Player.Role
        
        let index: Int
        let role: Role
        let word: String
        let name: String
        let image: Image
        var eliminated: Bool = false
        
        init(gamePlayer: ImpostorGame.Player, atIndex index: Int) {
            self.index = index
            self.role = gamePlayer.role
            self.word = gamePlayer.word
            self.name = "Player \(index + 1)"
            self.image = PlayerImages.shared.images[index] ?? PlayerImages.defaultImage
            self.eliminated = gamePlayer.eliminated
        }
        
        init(role: Role, word: String, name: String, image: Image, eliminated: Bool) {
            self.role = role
            self.word = word
            self.name = name
            self.image = image
            self.eliminated = eliminated
        }
    }
}
*/
